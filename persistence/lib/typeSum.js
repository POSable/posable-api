var payments = require('./paymentQuery');

var typeSum = function() {
    try {
    //    take the result of the payments query array and iterate over it pushing amount value into a sumArray.  Then run sum on the sumArray.
    } catch (err) {
        console.log(err);
    }
};

module.exports = typeSum();